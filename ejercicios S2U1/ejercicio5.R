# Accedemos al precio de la pizza y pasta de nuestra lista de comidas 
cost[c(1,5)]

# Define temp
temp <- c(35, 88, 42, 84, 81, 30)
city <- c("Beijing", "Lagos", "Paris", "Rio de Janeiro", "San Juan", "Toronto")
names(temp) <- city

# Accede a las temperaturas de Paris y San Juan
