# Define el vector x
x <- c(1, 3, 5,"a")

# Nota que x es un vector de caracteres y R convirtió los números 1,3,5 en caracteres
x

# Redefine  `x` para que sea un vector numérico (obtendrás una warning pero esta ok)
# Recuerda la función `as.numeric`.
