#El siguiente código es el ejemplo de una función que suma los 
#números de 1 a n
example_func <- function(n){
  x <- 1:n
  sum(x)
}
#Así ejecutaríamos la suma de los primeros 100 números
example_func(100)

#Escribe una función con el nombre 'compute_s_n' con argumento n
#que realice la suma 1 + 2^2 + 3^2 +...+ n^2

#Reporta el valor de la suma para n=10
