#Recuerda que en R puedes definir funciones que reciban argumentos
#específicos y que retornen valores deseados
#Por ejemplo la siguiente función suma 1 al parámetro recibido
my_func <- function(x){
  y <- x + 1
  y
}
#El valor y es lo que retorna esta función
#El código anterior solo define esta función, para ejecutarla
#debemos por ejemplo hacer una llamada a la misma de la forma:
my_func(5)

#Ahora crea una función 'sum_n' que para cualquier valor de entrada n 
#crea un vector 1:n y luego computa la suma de estos enteros
#entre 1 y n


#Luego utiliza esta función creada para determinar la suma de los 
#enteros entre 1 y 5000
