library(dslabs)
data(murders)
# Ahora utilizaremos la función match() para identificar los estados
# con las abreviaciones AK, MI, e IA.

#Primero define un vector 'abbs' de caracteres con las 3 abreviaciones
#Recuerda que para que sean caracteres tienen
#que tener "" 

#Machea el vector abbs con murders$abb y guárdalo en 'ind'

#Imprime los nombres de los estados de 'ind'
