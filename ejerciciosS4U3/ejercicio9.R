#Utiliza código tidyverse para realizar lo equivalente a:
exp(mean(log(murders$population)))
#Utiliza el pipe para pipear el argumento en las funciones
#Utiliza el operador '.' Para acceder a la población
#El código deberá empezar con murders %>%
