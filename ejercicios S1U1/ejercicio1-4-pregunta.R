# Corre el siguiente código en la consola:
#n <- 1000
#x <- seq(1,n)
#sum(x)

#Basados en el resultado o en la función ayuda que crees hace la función 'sum()' y 'seq()'?
#Opción 1) sum() crea una lista de números y seq() los suma
#Opción 2) seq() crea una lista de números y sum los suma
#Opción 3) seq() calcula la diferencia entre dos argumentos y sum() calcula la suma entre 1 y 1000
#Opción 4) sum() siempre retorna el mismo valor
