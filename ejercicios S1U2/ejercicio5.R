#Correr el código que se encuentra a continuación
library(dslabs)
data(murders)

# Chequea la clase de la variable región del dataframe murders
class(murders$region)

# Determina el número de categorías o regiones incluidas en esta variable
# Recuerda que puedes utilizar la función levels para chequear las categorías en que se divide un factor anidándola con la función
# length para determinar el tamaño del vector.
