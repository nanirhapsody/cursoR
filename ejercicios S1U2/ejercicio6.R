#Correr el código que se encuentra a continuación
library(dslabs)
data(murders)
#La función `c()` es la abreviación de concatenar,
#acción de conectar ítems en una cadena
#Esta función agrega los textos a un único vector
# al cual llamaremos x
x <- c("a", "a", "b", "b", "b", "c")
# La función tabla obtiene como entrada un vector y retorna 
#la frecuencia de cada elemento único en el
table(x)

#Escribe una línea de código que retorne el número de estados por región
