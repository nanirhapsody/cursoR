#Correr el código que se encuentra a continuación
library(dslabs)
data(murders)
x <- c("a", "a", "b", "b", "b", "c")
# La 'c' en el código de arriba es la abreviación para "concatenar" 
# Acción de conectar items a una cadena. La función c conecta todos los strings
#Creamos una variable X para guardar esta cadena.
# Corriendo ahora la siguiente función vemos su comportamiento
table(x)

# Escribe una línea de código para visualizar el número de estados por región
