#Correr el código que se encuentra a continuación
library(dslabs)
data(murders)
# Para acceder a la variable población de la dataset murders utilizamos el código:
p <- murders$population 

# Para determinar la clase del objeto `p` realizamos:
class(p)

# Utilizando el accesor extrae la abreviación de los estados del dataset y asígnalo a una variable 'a'

# Determina la clase de a
