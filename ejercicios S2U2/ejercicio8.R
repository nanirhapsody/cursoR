# Utilizaremos un nuevo dataset que contiene datos de cuentas y algunos NA
library(dslabs)
data(na_example)

# La función is.na retorna un vector lógico que nos dice que entradas son NA
#Utiliza la función is.na para crear una variable 'ind' con los índices
# de las entradas que son NA is.na(na_example)

# Determina cuantos NA hay en el data set utilizando la función sum()
