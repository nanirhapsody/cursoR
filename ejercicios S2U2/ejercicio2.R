#Cargamos las librerias y datos a utilizar
library(dslabs)
data(murders)

#La función order() retorna el vector de índices o posiciones
#resultantes de ordenar el vector. 
# Por esto la función sort(x) y x[order(x)] tienen el mismo resultado

# Accede a la variable población del dataset y almacénala en pop

#Utiliza el comando order() para encontrar el vector de posiciones
#Que ordenarían la variable pop y guárdalo en una nueva variable 'ord'


#Encuentra el número de fila del estado con la menor población
